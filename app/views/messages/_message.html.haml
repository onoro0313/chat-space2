%li.chat-body__block
  .chat-body__block_name
    = message.user.name
  .chat-body__block_date
    = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .chat-body__block_text
    = message.body
    = image_tag message.image.to_s
